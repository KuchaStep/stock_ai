import 'package:flutter/material.dart';

class AppColors {
  // ブランドカラー
  static const Color primary = Color(0xFF0B3C91);

  // 背景
  static const Color background = Color(0xFFF5F7FA);

  // カード
  static const Color card = Colors.white;

  // 上昇
  static const Color gain = Color(0xFFE53935);

  // 下落
  static const Color loss = Color(0xFF1565C0);

  // 文字
  static const Color text = Color(0xFF222222);

  static const Color subText = Color(0xFF666666);

  // 枠線
  static const Color border = Color(0xFFE0E0E0);
}